[project]
name = "knotodo"
version = "0.1.0"
description = "Minimal calendar and todo prototype"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.121.1",
    "filelock>=3.20.0",
    "pydantic>=2.12.3",
    "uvicorn>=0.38.0",
]
